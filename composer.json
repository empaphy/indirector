{
    "name": "empaphy/stream-wrapper",

    "require": {
        "php":           ">=7.2",
        "rector/rector": "^0.18"
    },

    "autoload": {
        "psr-4": {
            "Empaphy\\Indirector\\": "src/"
        }
    },

    "minimum-stability": "dev",
    "prefer-stable":     true,

    "config": {
        "platform": {
            "php": "7.2.0"
        }
    }
}
